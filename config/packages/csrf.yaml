# Stateful CSRF for forms (traditional, no JS required)
# Stateless CSRF kept for login/logout (uses the csrf-protection Stimulus controller)
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - submit
            - authenticate
            - logout
